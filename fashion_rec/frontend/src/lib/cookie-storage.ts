/**
 * Cookie helpers — re-exported from shared @hdz/auth for backward compatibility.
 */
export {
  getTokenFromCookie,
  setTokenInCookie,
  removeTokenFromCookie,
  webTokenStorage,
  AUTH_TOKEN_KEY,
} from '@hdz/auth'
